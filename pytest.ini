[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    network: tests requiring internet access (deselect with -m "not network")
    epftoolbox: tests requiring the epftoolbox package to be importable (deselect with -m "not epftoolbox")
